import React from 'react';
import './Home.css';

const Home = () => {
    return (
        <div className="home-container">
            {/* Page avec le dégradé global (fade linear gradient) */}
        </div>
    );
};

export default Home;
